#include "main.h"

/**
 * _abs - absolute value
 *
 * @n: n is an integer
 *
 * Return: Always (success) absolute value of a number
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
